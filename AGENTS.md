# Agent instructions

## Git / GitHub
- **Do not push to GitHub automatically.** Always wait for the user to explicitly ask before running `git push`.
- Test changes locally first (`npm run dev`), confirm with the user, then push when asked.
